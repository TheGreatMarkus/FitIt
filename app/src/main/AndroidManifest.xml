<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        package="com.ui.fitit">

    <dist:module dist:instant="true" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_fitit_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_fitit_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".ui.GroupActivity"
                android:label="@string/group_activity_title" />
        <activity
                android:name=".ui.LoginActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".ui.main.MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar" />
        <activity
                android:name=".ui.newevent.NewEventActivity"
                android:label="@string/new_event_title" />
        <activity
                android:name=".ui.FeedbackActivity"
                android:label="@string/feedback_title"
                android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>